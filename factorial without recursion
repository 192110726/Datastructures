
#include<stdio.h>
int main()
{
	int num,fact=1,i;
	printf("enter a number: ");
	scanf("%d",&num);
	for(i=1;i<=num;i++)
	{
		fact=fact*i;
	}
	printf("the factorial num is %d  ",fact,num);
	return 0;
}
